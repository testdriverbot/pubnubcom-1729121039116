version: 4.0.69
steps:
  - prompt: Click on "Solutions" in the header.
    commands:
      - command: hover-text
        text: Solutions
        description: link in the header
        action: click
  - prompt: Assert that the solutions offered by the service are listed.
    commands:
      - command: assert
        expect: the solutions offered by the service are listed
  - prompt: >-
      Validate that specific solution details can be accessed.  # Use the
      content of the file from the environment
    commands:
      - command: hover-text
        text: Chat
        description: solution under Solutions menu
        action: click
