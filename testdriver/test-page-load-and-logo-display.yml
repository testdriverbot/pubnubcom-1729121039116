version: 4.0.69
steps:
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: The complete real-time developer platform is visible
  - prompt: >-
      Check that the PubNub logo is visible on the page.  # Use the content of
      the file from the environment
    commands:
      - command: assert
        expect: PubNub is visible
